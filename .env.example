# Optional: Require API key for all requests (except /health)
# PROXY_API_KEY=your-secret-key

# Optional: Enable debug logging
# DEBUG=true

# Optional: Enable model fallback on quota exhaustion
# FALLBACK=true

# Optional: Custom port (default: 8080)
# PORT=8080
